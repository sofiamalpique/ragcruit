[project]
name = "ragcruit-backend"
version = "0.1.0"
description = "Backend service for ragcruit."
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn>=0.30,<1.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0,<9.0",
]
